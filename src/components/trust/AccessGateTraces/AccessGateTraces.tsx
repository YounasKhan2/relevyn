import styles from "./AccessGateTraces.module.css";const traces=[["DENIED TRACE","Recruiter requests private Passport section directly",["Organization membership","Candidate ownership check","No application boundary","DENY"],"403 / NO ROUTE","private data stays private"],["ALLOWED TRACE","Recruiter opens the application snapshot submitted to their role",["Organization membership","Tenant + role scope","Application snapshot exists","ALLOW"],"SCOPED READ","snapshot only"]];export function AccessGateTraces(){return <section className={styles.root}>{traces.map((t,i)=><article key={t[0]} data-tone={i?"allow":"deny"}><div><b>{t[0]}</b><p>{t[1]}</p></div><div className={styles.steps}>{(t[2] as string[]).map(x=><span key={x}>{x}</span>)}</div><aside><b>{t[3]}</b><small>{t[4]}</small></aside></article>)}<footer>Policy is evaluated server-side. The client UI can reflect permissions, but it is never the authority for tenant or candidate-data access.</footer></section>}