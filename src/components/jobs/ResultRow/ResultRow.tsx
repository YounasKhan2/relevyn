import{classNames}from"@/lib/classNames";import styles from"./ResultRow.module.css";export type ResultRowProps={title:string;company:string;meta:string;skills:string;salary?:string;posted?:string;sourceLabel?:string;selected?:boolean;onClick?:()=>void;className?:string};export function ResultRow({title,company,meta,skills,salary,posted,sourceLabel,selected=false,onClick,className}:ResultRowProps){return <button type="button" onClick={onClick} className={classNames(styles.row,selected&&styles.selected,className)} aria-pressed={selected}><div className={styles.primary}><h3>{title}</h3><p className={styles.company}>{company}</p><p>{meta}</p><p className={styles.skills}>{skills}</p></div><div className={styles.secondary}>{salary&&<strong>{salary}</strong>}{posted&&<span>{posted}</span>}{sourceLabel&&<span className={styles.source}>{sourceLabel}</span>}</div></button>}
