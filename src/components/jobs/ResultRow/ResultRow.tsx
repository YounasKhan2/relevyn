import{SourceBadge,type JobSource}from"@/components/jobs/SourceBadge/SourceBadge";import{classNames}from"@/lib/classNames";import styles from"./ResultRow.module.css";
export type ResultRowProps={title:string;company:string;meta:string;skills:string;salary?:string;posted?:string;source?:JobSource;sourceLabel?:string;selected?:boolean;onClick?:()=>void;className?:string};
export function ResultRow({title,company,meta,skills,salary="$110k–140k",posted="Posted 2d ago",source="external",sourceLabel,selected=false,onClick,className}:ResultRowProps){return <button type="button" onClick={onClick} className={classNames(styles.row,selected&&styles.selected,className)} aria-pressed={selected}><div className={styles.primary}><h3>{title}</h3><p className={styles.company}>{company}</p><p className={styles.meta}>{meta}</p><p className={styles.skills}>{skills}</p></div><div className={styles.secondary}><strong>{salary}</strong><span>{posted}</span><SourceBadge source={source} label={sourceLabel}/></div></button>}
