import{useState}from"react";import{classNames}from"@/lib/classNames";import styles from"./ExternalApplyNotice.module.css";export type ExternalApplyNoticeState="guest"|"signedIn";export type ExternalApplyNoticeProps={state?:ExternalApplyNoticeState;company:string;sourceLabel:string;onCancel?:()=>void;onContinue?:(trackApplication:boolean)=>void;className?:string};export function ExternalApplyNotice({state="guest",company,sourceLabel,onCancel,onContinue,className}:ExternalApplyNoticeProps){const[track,setTrack]=useState(false);return <section className={classNames(styles.notice,state==="signedIn"&&styles.signedIn,className)}><p className={styles.eyebrow}>EXTERNAL APPLICATION</p><h3>Continue to {company}</h3><p className={styles.description}>This application is handled outside Relevyn. You’ll continue on the employer or source application system.</p><p className={styles.source}>Source: {sourceLabel} ↗</p>{state==="signedIn"&&<label className={styles.track}><input type="checkbox" checked={track} onChange={e=>setTrack(e.target.checked)}/><span>Add this role to my application tracker after I apply</span></label>}<div className={styles.actions}><button type="button" onClick={onCancel}>Cancel</button><button type="button" className={styles.continue} onClick={()=>onContinue?.(track)}>Continue</button></div></section>}
