import{classNames}from"@/lib/classNames";import styles from"./JobRow.module.css";export type JobRowProps={title:string;company:string;meta:string;skills:string;href?:string;selected?:boolean;className?:string};export function JobRow({title,company,meta,skills,href="#",selected=false,className}:JobRowProps){return <a href={href} className={classNames(styles.row,selected&&styles.selected,className)} aria-current={selected?"true":undefined}><div className={styles.content}><h3>{title}</h3><p className={styles.company}>{company}</p><p className={styles.meta}>{meta}</p><p className={styles.skills}>{skills}</p></div><span className={styles.arrow} aria-hidden="true">↗</span></a>}
