import type{ButtonHTMLAttributes}from"react";import{classNames}from"@/lib/classNames";import styles from"./FilterTrigger.module.css";export type FilterTriggerProps=ButtonHTMLAttributes<HTMLButtonElement>&{active?:boolean;label:string};export function FilterTrigger({active=false,label,className,...props}:FilterTriggerProps){return <button type="button" {...props} className={classNames(styles.trigger,active&&styles.active,className)} aria-pressed={active}><span>{label}</span><span className={styles.chevron} aria-hidden="true">⌄</span></button>}
