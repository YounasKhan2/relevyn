import{classNames}from"@/lib/classNames";import styles from"./RequirementGroup.module.css";
export type RequirementGroupType="required"|"preferred";export type RequirementGroupProps={type?:RequirementGroupType;items:string[];subtitle?:string;className?:string};
const TITLES:Record<RequirementGroupType,string>={required:"REQUIRED",preferred:"PREFERRED"};const SUB:Record<RequirementGroupType,string>={required:"Core criteria from the source listing",preferred:"Useful context, not a hard gate"};
export function RequirementGroup({type="required",items,subtitle,className}:RequirementGroupProps){return <section className={classNames(styles.group,className)} data-type={type}><p className={styles.title}>{TITLES[type]}</p><p className={styles.subtitle}>{subtitle??SUB[type]}</p><ul>{items.map(i=><li key={i}>{i}</li>)}</ul></section>}
