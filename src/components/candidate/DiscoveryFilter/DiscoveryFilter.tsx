import type{ButtonHTMLAttributes}from"react";import{classNames}from"@/lib/classNames";import styles from"./DiscoveryFilter.module.css";export type DiscoveryFilterProps=ButtonHTMLAttributes<HTMLButtonElement>&{active?:boolean;label:string};export function DiscoveryFilter({active=false,label,className,...props}:DiscoveryFilterProps){return <button type="button" {...props} className={classNames(styles.filter,active&&styles.active,className)} aria-pressed={active}><span>{label}</span><span aria-hidden="true">▾</span></button>}
