import{classNames}from"@/lib/classNames";import styles from"./DiscoveryLedgerRow.module.css";
export type DiscoveryLedgerRowProps={title:string;company:string;location:string;skills:string;source:string;verified:string;aligned:string;review:string;selected?:boolean;onClick?:()=>void;className?:string};
export function DiscoveryLedgerRow({title,company,location,skills,source,verified,aligned,review,selected=false,onClick,className}:DiscoveryLedgerRowProps){return <button type="button" onClick={onClick} className={classNames(styles.row,selected&&styles.selected,className)} aria-pressed={selected}><span className={styles.identity}><strong>{title}</strong><small>{company}</small></span><span className={styles.context}><small>{location}</small><small>{skills}</small></span><span className={styles.source}><small>{source}</small><small>{verified}</small></span><span className={styles.signal}><strong>{aligned}</strong><small>{review}</small></span><span className={styles.arrow}>→</span></button>}
