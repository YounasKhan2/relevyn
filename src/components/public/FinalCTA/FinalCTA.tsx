import{Button}from"@/components/core/Button/Button";import{classNames}from"@/lib/classNames";import styles from"./FinalCTA.module.css";export type FinalCTAProps={eyebrow?:string;title?:string;description?:string;primaryLabel?:string;primaryHref?:string;secondaryLabel?:string;secondaryHref?:string;watermark?:string;className?:string};export function FinalCTA({eyebrow="READY WHEN YOU ARE",title="Build once. Move with your work.",description="Create your Career Passport and keep one professional identity across discovery, applications and future opportunities.",primaryLabel="Create your passport",primaryHref="/signup",secondaryLabel="Explore for companies",secondaryHref="/companies",watermark="M",className}:FinalCTAProps){return <section className={classNames(styles.cta,className)}><span className={styles.watermark} aria-hidden="true">{watermark}</span><p className={styles.eyebrow}>{eyebrow}</p><h2>{title}</h2><p className={styles.description}>{description}</p><div className={styles.actions}><Button href={primaryHref}>{primaryLabel}</Button><Button href={secondaryHref} variant="secondary">{secondaryLabel}</Button></div></section>}
