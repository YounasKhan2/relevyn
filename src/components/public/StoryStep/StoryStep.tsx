import{classNames}from"@/lib/classNames";import styles from"./StoryStep.module.css";export type StoryStepProps={indexLabel:string;title:string;description:string;active?:boolean;className?:string};export function StoryStep({indexLabel,title,description,active=false,className}:StoryStepProps){return <article className={classNames(styles.step,active&&styles.active,className)}><p className={styles.index}>{indexLabel}</p><h3>{title}</h3><p className={styles.description}>{description}</p></article>}
