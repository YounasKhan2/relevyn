import{useState}from"react";import{Button}from"@/components/core/Button/Button";import{classNames}from"@/lib/classNames";import styles from"./PublicHeader.module.css";
export type PublicHeaderState="top"|"scrolled";export type PublicNavItem={label:string;href:string};export type PublicHeaderProps={state?:PublicHeaderState;brandHref?:string;navItems?:PublicNavItem[];loginHref?:string;primaryHref?:string;primaryLabel?:string;className?:string};
const DEFAULT_NAV=[{label:"Jobs",href:"/jobs"},{label:"Candidates",href:"/candidates"},{label:"Companies",href:"/companies"}];
export function PublicHeader({state="top",brandHref="/",navItems=DEFAULT_NAV,loginHref="/login",primaryHref="/signup",primaryLabel="Create passport",className}:PublicHeaderProps){const[menuOpen,setMenuOpen]=useState(false);return <header className={classNames(styles.header,state==="scrolled"&&styles.scrolled,className)}><a className={styles.brand} href={brandHref}>RELEVYN</a><nav className={styles.desktopNav} aria-label="Primary navigation">{navItems.map(i=><a key={i.href} href={i.href}>{i.label}</a>)}</nav><div className={styles.desktopActions}><a className={styles.login} href={loginHref}>Log in</a><Button href={primaryHref} size="lg">{primaryLabel}</Button></div><div className={styles.mobileActions}><a className={styles.login} href={loginHref}>Log in</a><button type="button" className={styles.menuTrigger} onClick={()=>setMenuOpen(v=>!v)} aria-expanded={menuOpen} aria-controls="public-mobile-nav">Menu</button></div><nav id="public-mobile-nav" className={classNames(styles.mobileMenu,menuOpen&&styles.mobileMenuOpen)} aria-label="Mobile navigation">{navItems.map(i=><a key={i.href} href={i.href} onClick={()=>setMenuOpen(false)}>{i.label}</a>)}<Button href={primaryHref} size="lg" fullWidth>{primaryLabel}</Button></nav></header>}
