import{classNames}from"@/lib/classNames";import styles from"./FAQEditorialRail.module.css";export type FAQEditorialLink={label:string;href:string};export type FAQEditorialRailProps={eyebrow?:string;title?:string;links?:FAQEditorialLink[];note?:string;className?:string};const DEFAULT=[{label:"How Relevyn works",href:"/about"},{label:"Trust & privacy",href:"/trust"},{label:"For hiring teams",href:"/companies"}];export function FAQEditorialRail({eyebrow="FAQ / RELEVYN",title="Questions that usually come up before you build your professional identity.",links=DEFAULT,note="Candidate-controlled identity and clear product boundaries remain part of the system.",className}:FAQEditorialRailProps){return <aside className={classNames(styles.rail,className)}><p className={styles.eyebrow}>{eyebrow}</p><h3>{title}</h3><nav className={styles.links} aria-label="FAQ related links">{links.map(l=><a key={l.href} href={l.href}>{l.label} <span aria-hidden="true">→</span></a>)}</nav><p className={styles.note}>{note}</p></aside>}
