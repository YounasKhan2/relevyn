import { ResumeReviewWorkspace } from "@/components/candidate/ResumeReviewWorkspace/ResumeReviewWorkspace";import shared from "../../candidate.shared.module.css";import styles from "./ResumeReviewSection.module.css";
export function ResumeReviewSection(){return <section className={shared.section+" "+styles.section}><p className={shared.eyebrow}>RESUME → CAREER PASSPORT</p><h2 className={shared.title}>AI can suggest. You decide what becomes part of your record.</h2><p className={shared.body}>Upload remains private. Extraction, OCR fallback and structured parsing can propose updates, but every career fact stays pending until you review it.</p><div className={styles.workspace}><ResumeReviewWorkspace/></div></section>}
