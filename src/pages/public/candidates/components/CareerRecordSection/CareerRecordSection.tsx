import { CareerRecordSpine } from "@/components/candidate/CareerRecordSpine/CareerRecordSpine";import shared from "../../candidate.shared.module.css";import styles from "./CareerRecordSection.module.css";
export function CareerRecordSection(){return <section className={shared.section+" "+styles.section}><p className={shared.eyebrow}>ONE RECORD / MANY CHANGES</p><h2 className={shared.title}>A career record that grows in place.</h2><p className={shared.body}>Instead of creating a new identity for every application, your Career Passport keeps one structured history and adds new context over time.</p><div className={styles.workspace}><CareerRecordSpine/></div></section>}
