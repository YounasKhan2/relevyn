import { RequirementAlignmentMatrix } from "@/components/candidate/RequirementAlignmentMatrix/RequirementAlignmentMatrix";import shared from "../../candidate.shared.module.css";import styles from "./OpportunityLensSection.module.css";
export function OpportunityLensSection(){return <section className={shared.section+" "+styles.section}><p className={shared.eyebrow}>OPPORTUNITY LENS</p><h2 className={shared.title}>See the role. Then see where your context helps.</h2><p className={shared.body}>The job description remains the source of truth. Relevyn can layer your approved Career Passport context alongside it—aligned, needs review, or not found.</p><div className={styles.matrix}><RequirementAlignmentMatrix/></div></section>}
