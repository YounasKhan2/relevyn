import shared from "../../landing.shared.module.css";import styles from "./Manifesto.module.css";
const principles=[["01","Candidate-controlled"],["02","Evidence-aware"],["03","Built for real workflows"]];
export function Manifesto(){return <section className={shared.section+" "+shared.brand+" "+styles.section}><span className={shared.watermark}>R</span><p className={shared.eyebrow}>A PRINCIPLE, NOT A FEATURE</p><h2 className={styles.title}>Your career should not reset every time you apply.</h2><p className={shared.body}>Build a professional identity once. Improve it over time. Carry the useful context forward instead of starting from another empty form.</p><div className={styles.principles}>{principles.map(([n,label])=><div key={n}><span>{n}</span><b>{label}</b></div>)}</div></section>}
