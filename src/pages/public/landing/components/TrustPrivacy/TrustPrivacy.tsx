import { PrivacyControlRow } from "@/components/candidate/PrivacyControlRow/PrivacyControlRow";import { Button } from "@/components/core/Button/Button";import shared from "../../landing.shared.module.css";import styles from "./TrustPrivacy.module.css";
const rows=[["Profile discovery","Verified employers only",true],["References","Private by default",false],["Compensation preference","Private until shared",false],["Application snapshot","Shared when you submit",true]] as const;
export function TrustPrivacy(){return <section className={styles.section}><span className={styles.watermark}>T</span><div className={styles.copy}><p className={shared.eyebrow}>TRUST IS PART OF THE PRODUCT</p><h2>Professional identity should come with clear boundaries.</h2><p>Privacy controls must be understandable at the point where data moves—from private career context into an application or employer workflow.</p><Button href="/trust" variant="secondary">Read our privacy approach</Button></div><div className={styles.demo}><h3>Career Passport visibility</h3><p>Control what is private, what can be used in discovery, and what crosses into a submitted application.</p>{rows.map(([label,description,enabled])=><PrivacyControlRow key={label} label={label} description={description} enabled={enabled}/>)}</div></section>}
